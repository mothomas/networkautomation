#  - name: Pause for 5 minutes to build app cache
#    ansible.builtin.pause:
#      minutes: 5  


  - name: "Add output directory to git"
    ansible.builtin.shell: |
      branch_name="29-03-24"
      git config --global user.email "mobthomas@gmail.com"
      git config --global user.name "mothomas"
        git init
      git remote add origin https://mothomas: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@github.com/mothomas/backup.git
      git fetch origin main
      git checkout -b "$branch_name" 
      git add .
      git commit -m " backup on "$branch_name" "
      git push origin "$branch_name"
      git checkout main
      git merge "$branch_name"
      git push origin main    
    args:
      chdir: /backup
    delegate_to: localhost
        
      
        
